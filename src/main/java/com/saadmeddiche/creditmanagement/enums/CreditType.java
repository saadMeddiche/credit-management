package com.saadmeddiche.creditmanagement.enums;

public enum CreditType {
    ON_ME, // mean the user need to pay the credit to person who grant it
    ON_OTHER // mean the user need to receive the credit from person who grant it
}
